<Page
    x:Class="DSAapp.Views.CuadrículaDeDatosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <controls:DataGrid
            AutoGenerateColumns="False"
            GridLinesVisibility="Horizontal"
            ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}">
            <controls:DataGrid.Resources>
                <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
            </controls:DataGrid.Resources>
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_FolioInterno" Binding="{Binding FolioInterno}" />
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_FolioOriginal" Binding="{Binding FolioOriginal}" />
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_Remitente" Binding="{Binding Remitente}" />
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_Asunto" Binding="{Binding Asunto}" />
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_UsuarioAsignado" Binding="{Binding UsuarioAsignado}" />
                <controls:DataGridTextColumn x:Uid="CuadrículaDeDatos_RutaArchivoRed" Binding="{Binding RutaArchivoRed}" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
